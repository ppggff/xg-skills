---
id: NNN
title: <change log for NNN>
project: <project>
created: YYYY-MM-DD
---

# Log NNN: <title> — 变更记录（只增不删 / append-only）

> **Reader: human**（审计/历史）——execution zone 里**唯二**给人读的产物之一（另一个是评审报告）；
> `resume`/Claude **从不读本文件**。详略按人读者写。参见 SKILL.md「Two zones」。
>
> **追加式**：每条 = 发生了什么变化 + **为什么**。过去的条目**不改不删**；纠错也是
> **新追加一条**（"先前 X，现改为 Y，因为 Z"），不要回去改旧条目。
>
> 分工：**当前状态**看 `progress.md`（快照）；**大决策**看 `adr/`（精选）；**完整经过与原因**
> 看这里（全量历史/审计）。**`resume` 不读本文件**（只增不删、可能很大，不在 resume 路径上）——
> 它纯给人读/审计。条目按日期分组，打类型标签便于扫读：
> `[需求] [设计] [纠错] [范围] [文档] [实现] [测试]`。
>
> `[实现]` 必记的一类：**plan task 被删/合并/降级，或已 `[x]` 的验收被作废**（plan 可自由改，
> 但删除不留痕会让 resume/review 分不清"做过没"——所以删除要记，单纯细化不必记）。

## YYYY-MM-DD

- `[类型]` 变化一句话。**原因**：为什么这么变 / 触发它的证据或反馈。
